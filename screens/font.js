// Fonts.js
import { StyleSheet } from 'react-native';

const Fonts = StyleSheet.create({
  heading: {
    fontFamily: 'LazyDog', 
    fontSize: 24,
  },
  body: {
    fontFamily: 'LazyDog', 
    fontSize: 16,
  },
  
});

export default Fonts;
