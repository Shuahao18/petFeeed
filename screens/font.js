// Fonts.js
import { StyleSheet } from 'react-native';

const Fonts = StyleSheet.create({
  heading: {
    fontFamily: 'LazyDog', // Replace with your font name
    fontSize: 24,
    fontWeight: 'bold',
  },
  body: {
    fontFamily: 'LazyDog', // Replace with your font name
    fontSize: 16,
    fontWeight: 'normal',
  },
  // Add more styles as needed
});

export default Fonts;
